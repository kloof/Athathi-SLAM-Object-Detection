"""Doors / windows / passage detection on refined wall segments.

Placeholder for M3 — populated with the openings-detection pipeline
(density-grid evidence + vision-band evidence + width/height priors +
deduplication) once the M2 line-segment merger lands.

Until then, the module exists only so the package layout is stable.
"""
